import 'package:flutter/material.dart';

class TabaraApp extends StatelessWidget {
  const TabaraApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}